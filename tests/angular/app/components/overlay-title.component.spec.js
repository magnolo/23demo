ngDescribe({
    name: 'Test overlay-title component',
    modules: 'app',
    element: '<overlay-title></overlay-title>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
