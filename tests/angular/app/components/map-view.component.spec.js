ngDescribe({
    name: 'Test map-view component',
    modules: 'app',
    element: '<map-view></map-view>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
