ngDescribe({
    name: 'Test app-navigator component',
    modules: 'app',
    element: '<app-navigator></app-navigator>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
