ngDescribe({
    name: 'Test mapboxgl component',
    modules: 'app',
    element: '<mapboxgl></mapboxgl>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
