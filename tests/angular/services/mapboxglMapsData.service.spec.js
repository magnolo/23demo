ngDescribe({
    name: 'Test MapboxglMapsDataService',
    modules: 'app',
    inject: 'MapboxglMapsDataService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
